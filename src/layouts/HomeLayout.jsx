import React from 'react';
import Header from '../components/Header';

const HomeLayout = () => {
    return (
        <div>
            <header>
                <Header></Header>
            </header>
            <main>
                {/* left side */}
                <section>

                </section>

                {/* main section */}
                <section>

                </section>

                {/* right side */}

                <section>

                </section>

            </main>
        </div>
    );
};

export default HomeLayout;